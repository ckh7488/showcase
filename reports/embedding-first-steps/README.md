# 임베딩 첫걸음

인터랙티브 입문 자료 v07의 정적 배포본입니다. 모든 학습 실험은 브라우저 안에서 계산하며 입력을 외부로 전송하지 않습니다. 마지막 문장 학습은 6개 키워드와 공통 선형 모델을 사용하는 교육용 모형이며 실제 언어 모델의 성능 시연이 아닙니다.

`index.html`은 같은 폴더의 `report.js`, `report.css`, `atlas.css`, `assets/iceberg.png`를 사용합니다. 공통 ATLAS 테마와 파비콘만 상위 assets에서 불러옵니다. 의존성 버전과 라이선스는 dependencies.json, THIRD_PARTY_NOTICES.md에 보존했습니다.
